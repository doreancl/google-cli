# dorean_g Backlog

## Deferred items

- Add an opt-in integration test suite guarded by build tags (not part of the default test run).
- Add GitHub Actions CI workflow at `.github/workflows/ci.yml` for push and pull request checks.
- Add richer terminal rendering abstractions for color and table output (`internal/ui`).
- Expand destructive-command policy and define exactly which commands require confirmation or `--force`.
